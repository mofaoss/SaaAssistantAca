# Asset Migration Blocklist

The following image folders remain in `app/framework/ui/resources/images/` because ownership is not 100% certain and automatic move is intentionally blocked:

- `display/`
- `help/`
- `icons/`

Handled folders were copied to module-local paths under `app/features/modules/*/resources/images/`.
